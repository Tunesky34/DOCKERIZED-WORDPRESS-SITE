name: Node CI
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        run: npm install
      - name: Install dependencies
      - name: Run tests
        run: npm test

